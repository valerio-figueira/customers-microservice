export enum RabbitMQServices {
  CUSTOMERS_RMQ = 'CUSTOMERS_RMQ',
}

export enum MessageBrokerQueues {
  CUSTOMERS_QUEUE = 'customers_queue',
}

export enum MessageBrokerExchanges {
  CUSTOMERS_EXCHANGE = 'customers_exchange',
}
